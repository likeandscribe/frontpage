{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["VERCEL_PROJECT_PRODUCTION_URL", "VERCEL_TOKEN"]
    }
  }
}
