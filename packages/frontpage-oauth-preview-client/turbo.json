{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "VERCEL_DEPLOYMENT_ID",
        "VERCEL_PROJECT_PRODUCTION_URL",
        "VERCEL_TOKEN"
      ]
    }
  }
}
