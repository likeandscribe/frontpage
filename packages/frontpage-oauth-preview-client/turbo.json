{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["VERCEL_DEPLOYMENT_ID", "VERCEL_PROJECT_PRODUCTION_URL"]
    }
  }
}
